# CLAUDE.md

This file is auto-loaded by Claude Code at session start.  Treat it
as the entry point; the substance is in the imported files.

The project conventions (build, style, tasks) and key settled
decisions live in [`AGENTS.md`](AGENTS.md) — Claude Code and human
collaborators read the same doc.

The per-session memory (architectural notes, verified results,
non-obvious gotchas) lives in `.agent-memory/`; its one-line index
is imported below so every new session sees the topics at hand and
can `Read` the underlying files on demand.

@.agent-memory/MEMORY.md
